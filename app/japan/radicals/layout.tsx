import { Metadata } from "next"
export const metadata: Metadata = { title: "한자 부수 플래시카드", description: "강희자전 214개 한자 부수 플래시카드 학습" }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
